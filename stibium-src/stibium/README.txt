This is currently a placeholder for the stibium package. Right now it is stored inside the server/
package for ease of testing. Later, once there are tests and the package is mature, we should
move out stibium and publish it as a separate package.